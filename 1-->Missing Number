class Solution {
    public int missingNumber(int[] nums) {
        int size=nums.length,sum=0,actualSum=0;
        sum=(size*(size+1))/2;
        for(int i=0;i<size;i++){
            actualSum+=nums[i];
        }
        return sum-actualSum;  
    }
}
